server:
  port: 3344

spring:
  application:
    name: microservice-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:tingxuelouwq/microservice-config.git
          search-paths:
      label: master

